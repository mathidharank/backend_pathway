#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../appoptics-apm/bin/node-ao" "$@"
  ret=$?
else 
  node  "$basedir/../appoptics-apm/bin/node-ao" "$@"
  ret=$?
fi
exit $ret
